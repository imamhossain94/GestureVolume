<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
      Optional. Only for the Do Not Disturb toggle, and only exercised once the user has granted
      Do Not Disturb access on the system screen the app sends them to when they pick that action.
    -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!--
      Optional, both runtime-prompted at the moment their switch is flipped on the Deck's search
      settings and never before: Contacts lets the search find people by name, Phone lets quick
      dial and search place a call without stopping at the dialer.
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--
      Optional, coarse only, and only for the Deck's weather tile: the last fix the phone already
      has is read, rounded to two decimals, and sent to open-meteo.com. No fine location, no
      background location, and nothing is stored.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- The flashlight tile. Declared optional so the app still installs on devices without one. -->
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <!--
      Package visibility for the Deck: the app-shortcut picker lists launcher apps, the search bar
      launches them, and the calculator tile prefers the device's own calculator.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_CALCULATOR" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
        <package android:name="com.google.android.googlequicksearchbox" />
    </queries>

    <!--
      Android 13+. The foreground service's notification is the app's remote control: Show/Hide,
      Settings and Stop, and the only route back to a bar put away with "Hide handler" while the
      app is closed. Without this permission that notification is silently withheld from the shade
      on 13+, so the controls exist and nobody can reach them.

      Asked for once, when the user first switches the service on, and never blocking: declining it
      costs the notification, not the app.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Optional. Only needed for the brightness swipe actions, and only requested at the moment the
      user selects one - never at startup. The app works fully without it.
    -->
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!--
      Note: REQUEST_IGNORE_BATTERY_OPTIMIZATIONS is deliberately NOT declared. The Troubleshoot
      screen sends the user to the system battery-optimisation list instead of popping the direct
      allow/deny dialog, which is the Play-policy-safe route for an app of this type - and declaring
      a sensitive permission the app never exercises is exactly what gets a listing flagged.
    -->

    <application
        android:name=".GestureApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestureVolume">
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:configChanges="locale|layoutDirection"
            android:theme="@style/Theme.GestureVolume">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Invisible, and started only by the Deck's microphone button: an overlay window cannot
          receive an Activity result, so this runs the system recogniser on its behalf and closes
          the moment it has an answer.
        -->
        <!-- The QR tile. Invisible for the same reason VoiceSearchActivity is. -->
        <activity
            android:name=".ui.activities.QrScanActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@style/Theme.GestureVolume.Transparent" />

        <activity
            android:name=".ui.activities.VoiceSearchActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@style/Theme.GestureVolume.Transparent" />

        <receiver
            android:name=".service.ServiceRestartReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
          stopWithTask="false" keeps the overlay alive when the user swipes the app out of Recents,
          which is the single most common way this service used to die.
        -->
        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="This_ForegroundService_Provides_SystemWide_Controls_For_User_Interaction"/>
        </service>

        <!--
          The accessibility service. Performs the system actions (lock, screenshot, Back, Home,
          Recents, notifications, quick settings, power menu), optionally draws the bar so that no
          notification is needed, and optionally captures copied text for the clipboard history.
          Everything it does is opt-in from inside the app, and the user switches it on themselves
          in system settings after an in-app disclosure. See res/xml/accessibility_service_config.
        -->
        <service
            android:name=".service.GestureAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>