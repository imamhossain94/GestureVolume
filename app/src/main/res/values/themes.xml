<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.GestureVolume" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!--
      For VoiceSearchActivity, which draws nothing: the system speech recogniser's own dialog is
      the only thing that should appear, over whatever the user was looking at.
    -->
    <style name="Theme.GestureVolume.Transparent" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>